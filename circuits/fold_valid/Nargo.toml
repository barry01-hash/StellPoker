[package]
name = "fold_valid"
type = "bin"
authors = ["Stellar Poker"]
compiler_version = ">=0.36.0"

[dependencies]
stellar_poker_lib = { path = "../lib" }

[package.metadata.circuit]
description = "Proves folded cards are removed from the active set and cryptographically burned (not reusable in later rounds) (Issue #217)."
max_players = 6
