[package]
name = "hole_card_range"
type = "bin"
authors = ["Stellar Poker"]
compiler_version = ">=0.36.0"

[dependencies]
stellar_poker_lib = { path = "../lib" }

[package.metadata.circuit]
description = "Prove that a player's private hole cards lie within a claimed rank range (Issue #211)."
max_players = 1
poseidon2_permutations = 3
