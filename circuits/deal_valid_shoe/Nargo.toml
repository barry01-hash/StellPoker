[package]
name = "deal_valid_shoe"
type = "bin"
authors = ["Stellar Poker"]
compiler_version = ">=0.36.0"

[dependencies]
stellar_poker_lib = { path = "../lib" }

[package.metadata.circuit]
description = "Deal validity for a 6-deck (312-card) shoe; verifies per-rank/per-suit distribution (Issue #221)."
max_players = 6
